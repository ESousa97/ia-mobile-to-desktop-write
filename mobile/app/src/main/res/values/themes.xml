<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base do sistema; a aparência real é definida por Compose (MaterialTheme). -->
    <style name="Theme.ClipBridge" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
