# Exemplo de configuração de assinatura release (copie para keystore.properties na raiz de mobile/)
# NUNCA commite keystore.properties — já está no .gitignore

storeFile=../path/to/release.keystore
storePassword=change-me
keyAlias=clipbridge
keyPassword=change-me
